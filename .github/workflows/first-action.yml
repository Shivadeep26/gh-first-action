name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World!"
      - name: Print something
        run: |
             echo "First output"
             echo "Second output"
      - name: Print Good bye
        run: echo "Good bye"
  
